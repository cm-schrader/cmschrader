{
  "name": "cmschrader",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    // "deploy": "git push origin `git subtree split --prefix dist 3d`:gh-pages --force"
  },
  "devDependencies": {
    "vite": "^2.6.4"
  },
  "dependencies": {
    "mathjs": "^10.0.0",
    "three": "^0.135.0"
  }
}
